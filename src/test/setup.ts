// Test Setup
// 测试设置

import '@testing-library/jest-dom';

// Global test setup
// 全局测试设置
beforeAll(() => {
    // Setup any global test environment
    // 设置任何全局测试环境
});

afterAll(() => {
    // Cleanup any global test environment
    // 清理任何全局测试环境
}); 