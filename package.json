{
  "name": "alpha-radar",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test:crawler": "ts-node src/test/crawler.test.ts",
    "test:cleaner": "ts-node src/test/cleaner.test.ts",
    "test:sentiment": "ts-node src/test/sentiment.test.ts",
    "test:notifier": "ts-node src/test/notifier.test.ts",
    "test:pipeline": "ts-node src/test/full-pipeline.test.ts",
    "meme-radar:example": "ts-node examples/meme-radar-example.ts",
    "meme-radar:dev": "ts-node -e \"import('./src/pipeline/meme-radar-pipeline').then(m => { const { MemeRadarPipeline } = m; const { loadConfig } = require('./src/config/meme-radar-config'); const config = loadConfig('development'); const pipeline = new MemeRadarPipeline(config); pipeline.initialize().then(() => pipeline.startMonitoring()); })\"",
    "meme-radar:scan": "ts-node -e \"import('./src/pipeline/meme-radar-pipeline').then(m => { const { MemeRadarPipeline } = m; const { loadConfig } = require('./src/config/meme-radar-config'); const config = loadConfig('development'); const pipeline = new MemeRadarPipeline(config); pipeline.initialize().then(() => pipeline.runScan()).then(console.log); })\""
  },
  "dependencies": {
    "next": "^14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "openai": "^4.28.0",
    "rss-parser": "^3.13.0",
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.19",
    "@types/react": "^18.2.57",
    "@types/react-dom": "^18.2.19",
    "ts-node": "^10.9.2"
  }
} 